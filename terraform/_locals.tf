# we can put any local variables in this file. Here we are going to set our project prefix name which will be used in all resource names
locals {
  prefix = "demo-sunny" #NO uppercase letters, will fail
}